<% 
  variant = local_assigns[:variant] || '' 
  chosen_variant = fetch_chosen_variant(@abex_features)
%>

<% if variant.present? && chosen_variant.present? && variant.eql?(chosen_variant) %>
  <%= yield %>
<% end %>
