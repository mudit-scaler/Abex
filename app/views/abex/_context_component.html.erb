<%
  flag_key = local_assigns[:flag_key] || ''
  variant = local_assigns[:variant] || 'Default'
%>

<div data-flag-key="<%= flag_key %>" data-flag-variant="<%= variant %>">
  <%= yield %>
  <%= abex_switch_stack(@eventsss) %>
</div>
